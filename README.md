# server -server-internship
### in the server part of this project, we can make the connection between postgresql and node js in a simple and understandable way


`Server(NodeJs)-PostgreSql(Database)-Connection`
